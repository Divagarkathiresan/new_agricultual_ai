# Use a long, unique random value in your real .env file. Never commit that file.
JWT_SECRET_KEY=replace-with-a-long-random-secret
# Optional: defaults to 60 minutes.
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
